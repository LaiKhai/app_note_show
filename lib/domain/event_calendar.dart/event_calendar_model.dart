class EventCalendar {
  final String name;
  final String local;
  final String price;
  final DateTime startDate;
  final DateTime endDate;

  EventCalendar(
      {required this.name,
      required this.local,
      required this.price,
      required this.startDate,
      required this.endDate});
}
